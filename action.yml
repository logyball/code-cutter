name: 'Hello World GH example'
description: 'Greet someone and record the time'
inputs:
  github-token:  
    description: 'token'
    required: true
# outputs:
  # time: # id of output
  #   description: 'The time we greeted you'
runs:
  using: 'node16'
  main: 'dist/index.js'